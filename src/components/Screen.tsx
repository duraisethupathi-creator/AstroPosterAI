import React,{ReactNode}from'react';import{SafeAreaView,ScrollView,StyleSheet,ViewStyle}from'react-native';import{theme}from'../theme';export function Screen({children,scroll=false,contentStyle}:{children:ReactNode;scroll?:boolean;contentStyle?:ViewStyle}){if(scroll)return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={[s.content,contentStyle]}>{children}</ScrollView></SafeAreaView>;return <SafeAreaView style={[s.safe,s.content,contentStyle]}>{children}</SafeAreaView>}const s=StyleSheet.create({safe:{flex:1,backgroundColor:theme.colors.bg},content:{padding:20}});
